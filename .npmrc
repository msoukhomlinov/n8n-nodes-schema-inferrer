# Use hoisted node-linker to fix ESM module resolution issues
# pnpm's default isolated linker breaks ESM relative imports in n8n-workflow
# hoisted creates a node_modules structure similar to npm/yarn that works with ESM
node-linker=hoisted

# Prevent pnpm from auto-installing peer dependencies
# Custom n8n nodes should use n8n-workflow from the main n8n installation
auto-install-peers=false
